<?xml version="1.0" encoding="UTF-8"?>
<features name="com.ofbizian">

    <feature name="camel-routes-demo" resolver="(obr)" version="${project.version}">
        <feature dependency="true" version="${camel-version}">camel</feature>
        <feature dependency="true" version="${camel-version}">camel-jaxb</feature>
        <feature dependency="true" version="${camel-version}">camel-blueprint</feature>
        <feature>camel-cxf</feature>
        <feature dependency="true" version="${cxf.version}">cxf</feature>
        <feature dependency="true" version="${fabric.version}">fabric-core</feature>
        <bundle>mvn:com.ofbizian/ticket-contract/${project.version}</bundle>
        <bundle>mvn:com.ofbizian/ticket-service/${project.version}</bundle>
        <bundle>mvn:com.ofbizian/incident-service/${project.version}</bundle>
    </feature>
</features>
